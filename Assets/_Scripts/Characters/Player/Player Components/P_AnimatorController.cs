using UnityEngine;

namespace MyCode.Player
{
    public class P_AnimatorController : MonoBehaviour
    {
        /*
        private Animator animator;
        private P_Movement p_movement;

        private Vector2 moveValue;

        void Start()
        {
            animator = GetComponent<Animator>();
            p_movement = GetComponent<P_Movement>();
        }
        
        private void Update()
        {
            moveValue = p_movement.GetSmoothMoveValue();

            animator.SetFloat("X Direction", moveValue.x);
            animator.SetFloat("Y Direction", moveValue.y);
        }
        */
    }
}